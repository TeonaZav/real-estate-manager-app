import styled from "styled-components";

export const SPannelWrapper = styled.div`
  display: inline-flex;
  align-items: center;
  height: 4.7rem;
  padding: 0.6rem;
  gap: 2.4rem;
  border-radius: 1rem;
  border: var(--border);
  background: var(--clr-bg-primary);
`;
