import styled from "styled-components";

export const SNotFound = styled.p`
  color: var(--clr-text-not-found);

  font-size: 2rem;
  font-weight: 400;
  line-height: normal;
  margin-top: 6.5rem;
`;
